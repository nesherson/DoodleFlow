export type DrawingRectangle = {
    visible: boolean,
    x1: number,
    y1: number,
    x2: number,
    y2: number
}

export type Rectangle = {
    id: string,
    x: number,
    y: number,
    width: number,
    height: number,
    stroke: string,
}